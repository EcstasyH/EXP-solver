(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "12345") (re.* (re.range "0" "9"))(str.to_re "6789"))))
(assert (= (mod (mod (str.to_int x) 107) 90) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
