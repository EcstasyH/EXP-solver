(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "89")) (re.+ (str.to.re "48"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 265861) 226806) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
