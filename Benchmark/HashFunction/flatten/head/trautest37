(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "79")) (re.+ (str.to.re "99")))))
(assert (= (mod (mod (str.to.int x) 6343) 4290) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
