(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "1")) (re.+ (str.to.re "40")))))
(assert (= (mod (mod (str.to.int x) 59377) 33581) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
