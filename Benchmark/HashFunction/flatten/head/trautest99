(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "66")) (re.+ (str.to.re "42")))))
(assert (= (mod (mod (str.to.int x) 160579) 127886) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
