(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "28")) (re.+ (str.to.re "58"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 420731) 209119) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
