(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "28")) (re.+ (str.to.re "95"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 541669) 366520) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
