(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.+ (str.to_re "32")) (re.+ (str.to_re "5"))(str.to_re "6789")) ))
(assert (= (mod (mod (str.to_int x) 3617) 1534) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
