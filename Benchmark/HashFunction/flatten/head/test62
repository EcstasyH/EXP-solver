(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "12345") (re.+ (str.to_re "42")) (re.+ (str.to_re "8")))))
(assert (= (mod (mod (str.to_int x) 12203) 3647) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
