(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "9")) (re.+ (str.to.re "41")))))
(assert (= (mod (mod (str.to.int x) 20599) 1337) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
