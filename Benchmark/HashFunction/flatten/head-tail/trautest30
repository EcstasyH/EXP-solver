(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "89")) (re.+ (str.to.re "87"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 7001) 3555) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
