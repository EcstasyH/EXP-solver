(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "99")) (re.+ (str.to.re "68"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 409) 143) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
