(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.+ (str.to_re "79")) (re.+ (str.to_re "82"))(str.to_re "6789")) ))
(assert (= (mod (mod (str.to_int x) 892559) 207797) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
