(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "87")) (re.+ (str.to.re "14")))))
(assert (= (mod (mod (str.to.int x) 687787) 684955) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
