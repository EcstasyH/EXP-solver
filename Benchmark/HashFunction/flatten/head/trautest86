(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "5")) (re.+ (str.to.re "75")))))
(assert (= (mod (mod (str.to.int x) 683087) 85458) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
