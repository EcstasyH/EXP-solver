(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "12345") (re.+ (str.to_re "55")) (re.+ (str.to_re "84"))(str.to_re "6789"))))
(assert (= (mod (mod (str.to_int x) 8117) 6525) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
