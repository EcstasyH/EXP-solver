(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "95")) (re.+ (str.to.re "75"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 3907) 2945) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
