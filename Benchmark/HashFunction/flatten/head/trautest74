(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "63")) (re.+ (str.to.re "78")))))
(assert (= (mod (mod (str.to.int x) 87187) 45642) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
