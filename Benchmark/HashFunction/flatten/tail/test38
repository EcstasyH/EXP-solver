(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.+ (str.to_re "78")) (re.+ (str.to_re "74"))(str.to_re "6789")) ))
(assert (= (mod (mod (str.to_int x) 7207) 5753) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
