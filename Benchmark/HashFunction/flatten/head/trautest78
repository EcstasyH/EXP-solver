(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "1")) (re.+ (str.to.re "48")))))
(assert (= (mod (mod (str.to.int x) 767017) 428506) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
