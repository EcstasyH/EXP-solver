(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.* (re.range "0" "9"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 74887) 66416) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
