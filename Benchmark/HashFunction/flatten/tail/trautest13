(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "96")) (re.+ (str.to.re "82"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 571) 139) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
