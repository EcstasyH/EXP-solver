(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "60")) (re.+ (str.to.re "88"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 53527) 15588) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
