(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (re.+ (str.to_re "28")) (re.+ (str.to_re "95"))(str.to_re "6789")) ))
(assert (= (mod (mod (str.to_int x) 541669) 366520) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
