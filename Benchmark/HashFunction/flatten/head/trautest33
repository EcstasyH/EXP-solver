(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "67")) (re.+ (str.to.re "88")))))
(assert (= (mod (mod (str.to.int x) 4139) 2200) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
