(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "30")) (re.+ (str.to.re "15")))))
(assert (= (mod (mod (str.to.int x) 231631) 153676) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
