(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "19")) (re.+ (str.to.re "44"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 31973) 5148) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
