(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "32")) (re.+ (str.to.re "13"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 444557) 378323) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
