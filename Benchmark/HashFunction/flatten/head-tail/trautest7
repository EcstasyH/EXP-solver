(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "96")) (re.+ (str.to.re "8"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 283) 157) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
