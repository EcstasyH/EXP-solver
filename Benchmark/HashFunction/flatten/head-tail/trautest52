(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "18")) (re.+ (str.to.re "56"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 15149) 8692) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
