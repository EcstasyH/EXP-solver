(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "98")) (re.+ (str.to.re "38"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 41887) 7289) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
