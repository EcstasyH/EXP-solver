(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "76")) (re.+ (str.to.re "6")))))
(assert (= (mod (mod (str.to.int x) 151241) 104866) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
