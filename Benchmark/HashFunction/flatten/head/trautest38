(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "44")) (re.+ (str.to.re "31")))))
(assert (= (mod (mod (str.to.int x) 8467) 7591) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
