(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "49")) (re.+ (str.to.re "29"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 480707) 190195) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
