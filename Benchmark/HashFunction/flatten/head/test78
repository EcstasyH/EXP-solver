(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "12345") (re.+ (str.to_re "1")) (re.+ (str.to_re "48")))))
(assert (= (mod (mod (str.to_int x) 767017) 428506) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
