(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "20")) (re.+ (str.to.re "85")))))
(assert (= (mod (mod (str.to.int x) 4817) 163) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
