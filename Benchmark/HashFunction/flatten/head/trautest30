(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "75")) (re.+ (str.to.re "21")))))
(assert (= (mod (mod (str.to.int x) 2131) 1214) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
