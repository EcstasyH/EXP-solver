(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "84")) (re.+ (str.to.re "6")))))
(assert (= (mod (mod (str.to.int x) 54583) 46477) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
