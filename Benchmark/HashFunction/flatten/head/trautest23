(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "27")) (re.+ (str.to.re "83")))))
(assert (= (mod (mod (str.to.int x) 389) 206) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
