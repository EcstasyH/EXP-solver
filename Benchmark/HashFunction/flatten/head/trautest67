(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "53")) (re.+ (str.to.re "60")))))
(assert (= (mod (mod (str.to.int x) 39971) 20487) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
