(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "12345") (re.+ (str.to_re "71")) (re.+ (str.to_re "20")))))
(assert (= (mod (mod (str.to_int x) 9161) 7551) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
