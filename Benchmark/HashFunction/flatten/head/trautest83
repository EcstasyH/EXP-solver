(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "8")) (re.+ (str.to.re "81")))))
(assert (= (mod (mod (str.to.int x) 616157) 614818) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
