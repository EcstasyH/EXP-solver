(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (re.+ (str.to.re "48")) (re.+ (str.to.re "62"))(str.to.re "6789"))))
(assert (= (mod (mod (str.to.int x) 62687) 60372) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
