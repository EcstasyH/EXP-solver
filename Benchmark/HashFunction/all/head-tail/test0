(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "1") (re.* (str.to.re "4")))))
(assert (= (mod (str.to-int x)  12) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
