(set-logic QF_SLIA)
(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in_re x (re.++ (str.to_re "12345") (re.+ (str.to_re "9")) (re.+ (str.to_re "41")))))
(assert (= (mod (mod (str.to_int x) 20599) 1337) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
