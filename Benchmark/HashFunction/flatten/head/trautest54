(set-option :produce-models true)
(declare-fun x () String)
(assert (str.in.re x (re.++ (str.to.re "12345") (re.+ (str.to.re "52")) (re.+ (str.to.re "93")))))
(assert (= (mod (mod (str.to.int x) 22157) 9404) 0))
(assert (< (str.len x) 100))
(check-sat)
(get-model)
